export default function text(words, position) {
  var txt = document.createElement('a-text'),
      props = {
        value: words,
        geometry: "primitive:plane",
        color: "blue",
        align: "center"
      };
}
